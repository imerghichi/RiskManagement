package com.management.risk.Mitigation;

/**
 * The enum Non plan response enum.
 */
public enum NonPlanResponseEnum {
    /**
     * Avoid non plan response enum.
     */
    avoid,
    /**
     * Transfer non plan response enum.
     */
    transfer,
    /**
     * Acceptance non plan response enum.
     */
    acceptance,
    /**
     * Monitoring non plan response enum.
     */
    monitoring
}
